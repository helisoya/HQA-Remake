playMusic(Exploration_2)
removeAllCharacters()
setBackground(MinesOutside)

if(rain = 1) changeSkybox(NightRain)

setInteractionChapter(Mines;Mines/Mines_Check)
setInteractionChapter(Exit;MAP-Vlatyr-Mines)

interact